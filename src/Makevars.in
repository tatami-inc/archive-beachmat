PKG_LIBS=@PKG_LIBS@

.PHONY: all libbeach

all: libbeach $(SHLIB)

libbeach: $(SHLIB)
	mkdir -p "${R_PACKAGE_DIR}/include"
	cp *.h "${R_PACKAGE_DIR}/include"
	ar rc libbeach.a *.o
	mv $< libbeach.so
	cp libbeach.* "${R_PACKAGE_DIR}/libs"

shlib-clean: clean

clean: 
	rm -f libbeach.*
